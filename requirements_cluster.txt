# Requirements for HPC Cluster (Param Shakti) - Local Models Only
# ================================================================
# This file is for running with LOCAL HuggingFace models.
# Does NOT include google-generativeai or streamlit to avoid protobuf conflicts.
#
# Install: pip install -r requirements_cluster.txt

# Core
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
loguru>=0.7.0

# CAMEL-AI (base only, no rag extras to avoid protobuf>=6 requirement)
camel-ai>=0.2.85

# HuggingFace / Local Model Inference
transformers>=4.40.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0,<6.0.0

# PyTorch (install separately with CUDA support for your cluster)
# pip install torch --index-url https://download.pytorch.org/whl/cu121
# torch>=2.0.0

# Vision (MedSAM-2)
numpy>=1.24.0
Pillow>=10.0.0
scikit-image>=0.22.0

# Knowledge Graph (optional - comment out if not using Neo4j)
neo4j>=5.0.0
