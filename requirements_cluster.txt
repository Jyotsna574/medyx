# Requirements for HPC Cluster - Local LLM + Agent Pipeline
# ================================================================
# For run_mas_diagnosis.py with ACTIVE_PROVIDER=local
#
# INSTALL:
#   1. conda create -n medyx python=3.10 -y && conda activate medyx
#   2. pip install torch --index-url https://download.pytorch.org/whl/cu121  # if using GPU
#   3. pip install -r requirements_cluster.txt
# ================================================================

# Core
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
loguru>=0.7.0

# CAMEL-AI framework
camel-ai>=0.2.85

# HuggingFace / Local LLM
transformers>=4.40.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Vision (placeholder - image loading, basic processing)
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
scikit-image>=0.22.0

# Neo4j (optional)
neo4j>=5.0.0
