# Requirements for HPC Cluster (Param Shakti) - Local LLM + Vision
# ================================================================
# For running run_mas_diagnosis.py with ACTIVE_PROVIDER=local
# Does NOT include streamlit, google-generativeai (avoids protobuf conflicts)
#
# Install order:
#   1. pip install -r requirements_cluster.txt
#   2. pip install torch --index-url https://download.pytorch.org/whl/cu121  (or cu118 for your GPU)
#
# ================================================================
# REQUIRED - Core + LLM + Vision
# ================================================================

# Core config
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.0
loguru>=0.7.0

# CAMEL-AI (base, no rag/graph-rag extras)
camel-ai>=0.2.85

# HuggingFace / Local Model Inference
transformers>=4.40.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Vision (MedSAM-2) - REQUIRED for run_mas_diagnosis
numpy>=1.24.0
Pillow>=10.0.0
scikit-image>=0.22.0

# ================================================================
# REQUIRED - Neo4j Knowledge Graph (no fallbacks)
# ================================================================
neo4j>=5.0.0
# Set NEO4J_URI, NEO4J_USERNAME, NEO4J_PASSWORD in env

# ================================================================
# PyTorch - Install separately for your CUDA version
# ================================================================
# Example for CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Example for CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
